<Page
    x:Class="Air_3550.Pages.CheckoutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Air_3550.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <RelativePanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock x:Name="titleText" FontSize="56" FontFamily="Sans-Serif" Text="Checkout"/>
        <InfoBar x:Name="outputInfo" IsOpen="False" RelativePanel.Below="titleText"/>
        <TextBlock x:Name="departInfo" FontSize="24" RelativePanel.Below="outputInfo"/>
        <TextBlock x:Name="returnInfo" FontSize="24" RelativePanel.Below="departInfo"/>
        <TextBlock x:Name="summaryInfo" FontSize="24" RelativePanel.Below="returnInfo"/>
        <Button x:Name="useCredit" RelativePanel.Below="summaryInfo" Click="useCredit_Click" Visibility="Collapsed">Make Purchase with Credit</Button>
        <Button x:Name="usePoints" RelativePanel.Below="summaryInfo" Click="usePoints_Click" Visibility="Collapsed" RelativePanel.RightOf="useCredit">Make Purchase with Points</Button>
        <Button x:Name="useCreditCard" RelativePanel.Below="summaryInfo" Click="useCreditCard_Click" RelativePanel.RightOf="usePoints">Make Purchase with Credit Card</Button>
    </RelativePanel>
</Page>
